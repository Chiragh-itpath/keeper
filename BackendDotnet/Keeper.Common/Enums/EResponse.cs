namespace Keeper.Common.Enums
{
    public enum StatusType
    {
        OK,
        ALREADY_EXISTS,
        NOT_FOUND,
        NOT_AUTHORISED,
        NOT_VALID,
        INTERNAL_SERVER_ERROR
    }
}
